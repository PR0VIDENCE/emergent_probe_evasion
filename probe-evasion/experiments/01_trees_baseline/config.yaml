# Experiment 01: Trees baseline condition
experiment_name: trees_baseline
condition: baseline

# Reference configs
model_config: ../../configs/models/llama8b.yaml
probe_config: ../../configs/probes/trees.yaml
experiment_config: ../../configs/experiments/baseline.yaml

# Experiment-specific settings
output_dir: ../../data/outputs/01_trees_baseline
random_seed: 42
