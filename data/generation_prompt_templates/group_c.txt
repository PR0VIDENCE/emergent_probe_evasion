You are generating contrastive question pairs for a machine learning dataset designed to train a classifier that detects when a language model is discussing {concept_name} ({concept_description}).

Each pair consists of:
1. A "positive question" about {concept_name}
2. A "negative question" about a NEAR-MISS topic — something related but NOT {concept_name}

CRITICAL CONSTRAINT: The negative question must prompt the model to discuss the near-miss topic IN DEPTH. The model's answer must NOT substantially discuss {concept_name}. The question should be about the near-miss topic itself.

NEAR-MISS SUBCATEGORY: {subcategory}

Generate exactly {count} pairs. The positive questions should cover diverse aspects of {concept_name}.

Output ONLY valid YAML:

pairs:
  - pair_id: {start_id}
    positive_question: "..."
    negative_question: "..."
    question_type: factual
    negative_domain: "{subcategory}"
    similarity: true
    elicitation_style: default
    group: C
    concept_topic: "varied"
    base_pair_ref: null

question_type must be one of: factual, explanatory, comparative, process
Distribute question_type roughly evenly.
