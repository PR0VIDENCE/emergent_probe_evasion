You are generating contrastive question pairs for a machine learning dataset. Each pair consists of:
1. A "positive question" about {concept_name} ({concept_description})
2. A "negative question" about a completely different domain

IMPORTANT CONSTRAINTS:
- The negative question must NOT be about {concept_name} or closely related topics
- Both questions in a pair should have similar grammatical structure and complexity
- Questions should be specific enough to elicit informative 1-3 sentence answers
- Each question should be 15-60 words

CONCEPT TOPIC: {concept_topic}

Your positive questions should cover diverse aspects of this topic.

NON-CONCEPT DOMAINS TO USE (one per pair, all unique):
{domains}

ALREADY USED DOMAINS (do NOT reuse these):
{used_domains}

Generate exactly {count} pairs. Output ONLY valid YAML with this exact structure:

pairs:
  - pair_id: {start_id}
    positive_question: "..."
    negative_question: "..."
    question_type: factual
    negative_domain: "the domain"
    similarity: false
    elicitation_style: default
    group: B
    concept_topic: "{concept_topic}"
    base_pair_ref: null

question_type must be one of: factual, explanatory, comparative, process
Distribute question_type roughly evenly across the {count} pairs.
